
3 章 计算机科学
为什么学量子计算的人，必须先花时间学习经典计算机科学

1 借用成熟的概念与工具：
2 提供对比的“基准线（Benchmark）”：
3 培养“计算机科学家”的思维方式：  时间 和 空间 复杂度

3.1 计算模型

1. 引动机:
   - 目的: 给出“算法”与“可计算性”的精确数学刻画。
   - 历史突破: 丘奇-图灵论题 (Church-Turing thesis) 证明了算法的极限。

2. 两种模型的等价性:
   - 图灵机模型 ≡ 电路模型 (两者在可计算能力上完全等价)。
   - 习惯选择: 量子计算研究中主要使用【电路模型】(量子电路/量子门)。

3.1.1 图灵机

1. 图灵机模型 (Turing Machine):
   - 四要素: 程序 + 有限状态控制器 + 无限纸带 (内存) + 读写头。
   - 意义: 经典通用计算的理论基石。
1 通用图灵机（UTM）与“停机问题”
UTM（类似于现代操作系统）：可以把“程序代码本身”也当成数据写进纸带里传进去，实现“一台机器运行任意程序”。
停机问题（Halting Problem）：图灵证明了不存在任何一个通用算法，能够判断任意给定的程序到底会在有限时间内执行完（停机），还是会陷入死循环。这证明了经典计算能力的边界。

2 丘奇-图灵论题（Church-Turing Thesis）：
结论：“凡是人类直觉上认为‘可以按步骤算出’的数学函数，图灵机都一定能算出来”。图灵机就是算法能力的终极代名词。

图灵机（Turing Machine, $M$）：相当于一台只懂执行特定指令的硬件机器/CPU。它拥有固定的控制逻辑、纸带读写头和状态转移规则。

图灵机编号（Turing Number, $x$）：相当于存储在硬盘或纸带上的程序代码（比如 .exe 二进制文件，或者源码文本）。

3.1.2 电路

从 “图灵机（纸带+读写头）” --> 数字电路（导线+逻辑门）
1. 经典电路的 5 个基本要素
1 导线（Wires）：传输 0 或 1 的比特状态。
2 辅助比特（Ancilla bits）：额外准备的临时存储空间（常初始化为 0 或 1）。
3 扇出（FANOUT）：把 1 个比特复制成 2 个副本（即信号分叉，如 $a \to (a, a)$）。
4 交叉（CROSSOVER）：交换两条导线上的比特位置。
5 逻辑门（Logic Gates）：
    单比特门：非门（NOT，把 $a$ 变成 $1 \oplus a$）。
    双比特门：与门（AND）、或门（OR）、异或门（XOR，模 2 加法）、与非门（NAND）。
    重要规则：电路必须是无环的（Acyclic），不能有反馈回路，避免状态不稳定。
2. 怎样用小逻辑门搭建复杂计算？
半加器（HA, Half-Adder）：用 1 个 XOR 门算和（$x \oplus y$），1 个 AND 门算进位（$xy$）。
全加器（FA, Full-Adder）：用 2 个半加器（HA）和 1 个 OR 门，实现带上一步进位 $c$ 的 3 比特加法。
通用函数计算：利用数学归纳法（图 3-8），证明了只要用 NOT、AND、XOR 这些基础门，就能组合出计算任意布尔函数 $f: \{0,1\}^n \to \{0,1\}$ 的电路！
3. 核心概念：一致电路族
图灵机是“无限的”（一台图灵机能处理任意长度 $n$ 的输入）。
电路是“有限的”（一个固定电路只能处理固定长度 $n$ 的输入）。
那么，电路模型怎么去和图灵机比较计算能力呢？
引入：一致电路族（Uniform Circuit Family, $\{C_n\}\rangle$
结论：一致电路族的计算能力 = 图灵机的计算能力。两者在理论上是完全等价的。
4.  障碍
电路理论推广到量子力学时，会遇到三个巨大的量子物理限制：
1 复制限制（对应扇出 FANOUT）：经典电路可以随意复制信号，但量子力学有不可克隆定理，量子比特不能被无缝复制！
2 可逆性限制（对应逻辑门）：经典的 AND、OR 门都是不可逆的（抹掉了信息，会产生热耗散），但量子力学的薛定谔演化必须是酉变换（完全可逆的）！
3 量子门的重新设计：因为上述限制，第 4 章必须重新设计一套专门用于量子的“可逆逻辑门”（如 CNOT 门、Toffoli 门）。

3.2 计算问题分析
这一节是从“怎么构造计算（图灵机/电路）”转向“如何评估计算的代价”
1. 核心视角转变:
   - 从【可计算性】(能不能算) 转向 【计算复杂度】(算得有多快/需要消耗多少资源)。
2. 计算分析的三大基本问题:
   - 什么是计算问题: 将实际任务抽象为数学模型 (重点关注只有 Yes/No 答案的【判定问题】)。
   - 如何设计算法: 寻找保证正确解决问题的具体步骤。
   - 最小资源消耗: 评估解决问题最少需要消耗多少代价。
3. 三大核心计算资源:
   - 时间 (Time): 算法执行的步骤数 / 电路中的逻辑门数量。
   - 空间 (Space): 存储数据所需的内存大小 / 量子比特数量。
   - 能量 (Energy): 擦除信息与执行计算所消耗的物理能量 (引出可逆计算)。
4. 量子计算的本质价值:
   - 量子计算机并没有扩展“可计算”的边界，而是通过量子叠加与干涉，大幅度【降低了解决某些困难问题所需的计算资源 (主要是时间维度)】。

3.2.1 量化计算资源
大 $O$ 渐近记号（Asymptotic Notation）
1. 核心思想: 
   - 忽略常数因子与低阶项，专注当输入规模 n 趋于无穷大时，资源消耗的【增长速率】。

2. 三种渐近记号定义:
   - O(g(n)) [大 O]: 算法资源的【渐近上界】( Worst-case，最长耗时不超过它)。
   - Ω(g(n)) [大 Omega]: 问题的【渐近下界】( 理论极限，解决该问题至少需要花费的代价)。
   - Θ(g(n)) [大 Theta]: 【紧确界】( 当上界 O 与下界 Ω 在数量级上相等时，f(n) = Θ(g(n)))。

3. 常见复杂度数量级对比 (由快到慢):
   - Θ(1) < Θ(log n) < Θ(n) < Θ(n log n) < Θ(n^k) [多项式] < Θ(2^n) [指数]

4. 经典应用示例 (排序问题):
   - 传统冒泡排序: Θ(n^2)
   - 基于比较的排序理论下界: Ω(n log n)
   - 最优排序算法复杂度: Θ(n log n)

3.2.2 计算复杂性

1. 核心划分标准 (以输入规模 n 刻画):
   - 易解问题 (Tractable): 存在多项式时间 O(p(n)) 算法 (如加法 O(n)、排序 O(n log n))。
   - 难解问题 (Intractable): 最优算法需要指数时间 O(c^n) (如质因子分解)。

2. 强丘奇-图灵论题 (Strong Church-Turing Thesis):
   - 内容: 任意合理的经典计算模型，均可在多项式时间开销内被概率图灵机模拟。
   - 物理意义: 经典物理框架下的计算模型在【可兼顾的计算效率】上是等价的，无法凭硬件改进实现指数级跨越。

3. 量子计算的颠覆性:
   - 量子计算机打破了【强丘奇-图灵论题】。
   - Shor 算法将质因子分解这一经典【指数级难解问题】，降维打击为量子【多项式级易解问题】。

3.2.3 判定性问题与复杂性类P与NP

1. 核心复杂性类定义:
   - P (多项式时间): 经典计算机能在多项式时间内【求解】的问题 (易解问题)。
   - NP (非确定性多项式时间): 经典计算机能在多项式时间内【验证答案】的问题 (易验证问题)。
   - 包含关系: P ⊆ NP。
   - coNP：当问题的答案为【否 (No)】时，存在简短证据 (反例) 可以在【多项式时间内被验证】。
2. 归约 (Reduction) 与 NP 完全 (NP-Complete, NPC):
   - 归约: 将问题 B 在多项式时间内转化为问题 A (记作 B ≤ A)，说明 A 难度 ≥ B。
   - NP 完全问题 (NPC): NP 类中最难的问题集合 (如 CSAT, 3SAT, TSP)。
   - Cook-Levin 定理: 证明了第一个 NPC 问题 (CSAT)，建立了 NP 完全性理论。
3. 千禧年难题: P vs NP:
   - 学界普遍猜想 P ≠ NP (验证答案比直接求解更容易)。
   - 若任一 NPC 问题找到多项式算法，则 P = NP 成立。
4. NPI (NP 中间类) 与量子计算的精准定位:
   - NPI: 介于 P 与 NPC 之间的困难问题 (如【质因数分解】)。
   - 量子计算的边界:
     1. 量子计算机【大概率无法】在多项式时间内解决 NPC 问题。
     2. 量子计算机【擅长攻破 NPI 问题】(如 Shor 算法将质因数分解从指数级降为多项式级)。
     3. 人还在探索 NPC 问题
    
5. 几个著名问题
    1. 哈密顿圈HC：要求你从某个城市出发，必须且只能经过每一个城市恰好一次，最终还要飞回出发的那个城市。
    2. 旅行商问题（TSP）：找一条经过所有城市且总距离最短的路线
    3. 电路可满足性问题CSAT：是否存在一组特定的输入赋值（比如把输入设为 $0110...$），能够使得这个电路的最终输出为 $1$（True）？   --- NPC 问题
    4. SAT 类问题 ： 给定一个由布尔变量（只能取 $0$ 或 $1$）、逻辑联结词（$\land$ 与、$\lor$ 或、$\neg$ 非）以及括号构成的逻辑公式 $\phi$。是否存在一组变量的赋值（比如 $x_1=1, x_2=0, \dots$），使得整个公式的最终计算结果为 $1$（True）？
    5. 3SAT 问题（3-CNF Satisfiability，3-合取范式可满足性问题）
        子句（Clause）：几个变量（或其否定）用 “或 ($\lor$)” 连起来。例如 $(x_1 \lor \neg x_2 \lor x_3)$。
        CNF 公式：把若干个子句用 “与 ($\land$)” 串联起来。例如：

3.2.4 更多的复杂性类

1. 空间复杂性类:
   - PSPACE: 多项式空间复杂度类 (内存可复用，包含 P 与 NP)。多项式大小的内存空间 $O(n^k)$ 内解决的问题
   - 包含链条: L ⊆ P ⊆ NP ⊆ PSPACE ⊆ EXP。
   $\text{L}$（对数空间）与 $\text{EXP}$（指数时间）：
   $\text{L}$：只需要极微小的对数级内存 $O(\log n)$ 就能解决（非常省内存）。
   $\text{EXP}$：需要指数时间 $O(2^{n^k})$ 才能解决（已知极其困难的问题）。

2. 概率复杂性类 (BPP):
   - 定义: 允许抛硬币 (随机化)，多项式时间内以 ≥ 3/4 概率输出正确答案。
   - 物理意义: 刻画了经典物理下【实际可行计算】的真实上限。
   - 切诺夫界 (Chernoff Bound): 证明通过多次重复运行取多数票，错误率可呈指数级压低至可忽略不计。

3. 量子复杂性类 (BQP) 与量子算力定位:
   - BQP: 量子计算机能在多项式时间内以 ≥ 3/4 概率解决的问题类。
   - 相对关系: BPP ⊆ BQP ⊆ PSPACE。
   - 关键结论: 
     * Shor 算法证明了存在属于 BQP 但不属于 BPP 的问题 (质因数分解)。
     * 量子算力突破了经典 BPP 的界限，但仍受限于 PSPACE 框架内。

3.2.5 能量与计算

1. 物理本质 (兰道尔原理 Landauer's Principle):
   - 计算耗能的根源在于【不可逆的信息擦除】。
   - 擦除 1 比特信息消耗的能量下限为: E_min = k_B * T * ln(2)。
   - k_B 是玻尔兹曼常数，T 是 电脑工作环境温度
2. 可逆计算 (Reversible Computation):
   - 若计算过程每一步皆可逆 (无信息丢弃)，理论上能实现【零能量消耗】。
   - 经典可逆门代表: 
     * Fredkin 门 (台球计算机模型)
     * Toffoli 门 (双控制非门 CCNOT，具备通用计算能力)

3. 垃圾比特与还原技术 (Uncomputing):
   - 问题: 可逆计算会产生中间冗余的“垃圾比特” g(x)，若强行擦除会触发热耗散 (且会破坏量子干涉)。
   - 解决方案 (三步法):
     1. 计算得到 (x, f(x), g(x))
     2. 复制结果到输出位 y ⊕ f(x)
     3. 逆向运行前半电路 (Uncompute)，将 g(x) 还原归零。
   - 最终标准变换: (x, y) ↦ (x, y ⊕ f(x))。

4. 对量子计算的含义:
   - 证明了【酉变换 (可逆限制)】完全不会削弱通用计算能力。
   - 提供了在量子算法中消除“干扰干涉的冗余信息”的标准手段。

Fredkin 门（也叫控制交换门，Controlled-SWAP gate）是由物理学家爱德华·弗雷德金（Edward Fredkin）提出的一种三比特经典/量子通用可逆逻辑门。
它的直观物理含义非常简单：“看老板眼色行事”。
门有 3 个输入比特：1 个控制位 $c$，以及 2 个目标位 $a$ 和 $b$。
如果控制位 $c = 0$：目标位 $a$ 和 $b$ 保持原样，什么都不变。
如果控制位 $c = 1$：交换目标位 $a$ 和 $b$ 的值。
控制位 $c$ 在整个过程中永远保持不变。

Toffoli 门（托菲利门，也叫 CCNOT 门，即 Controlled-Controlled-NOT gate）是由计算机学家托马斯·托菲利（Tommaso Toffoli）提出的一种三比特通用可逆逻辑门。
如果说 Fredkin 门是“看老板眼色决定要不要交换”，那么 Toffoli 门就是“有两个老板同时点头，才翻转第三个人”。
Toffoli 门包含 2 个控制位（$a, b$） 和 1 个目标位（$c$）：
控制条件：当且仅当两个控制位同时为 1（即 $a = 1$ 且 $b = 1$）时，目标位 $c$ 才会发生翻转（$0 \to 1$ 或 $1 \to 0$）。
其余情况：只要有一个控制位为 0，三个比特全部保持不变。

注意: 兰道尔原理 针对的是 计算的过程，但是计算机本身工作 需要大量的 非计算功能，所以无论是什么计算机，都是耗能大户
【经典计算机能耗】 = 逻辑不可逆擦除热(微弱) + 晶体管电阻/漏电/电容充放电(主要) + 冷却风扇/水冷(辅助)
【量子计算机能耗】 = 逻辑演化能耗(0) + 量子测量/纠错擦除(次要) + 液氦超低温制冷/微波控制仪器(绝对主力)

3.3 关于计算机科学的观点

1. 各种经典替代计算模型的评估:
   - 并行计算 (Parallel): 空间代价抵消时间优势，无法突破图灵机多项式界限。
   - DNA 计算 (DNA): 利用生物分子高并发穷举，但分子需求量随规模指数暴涨，本质仍是经典搜索。
   - 模拟计算 (Analog): 利用连续变量，但物理【噪声】的存在将其算力压回离散图灵机水平。
   - 分布式计算：利用网络把地理位置分散的多台计算机连在一起合作。衍生出了通信复杂度（Communication Complexity）——评估节点之间传输数据的网络成本。

2. 物理与信息的交汇 (麦克斯韦妖 Maxwell's Demon):
   - 小恶魔“降熵”违背热力学第二定律的悖论，最终由【兰道尔原理】解决。
   - 小恶魔擦除内部存储记忆的动作必然向环境散热，保证了总熵增加。
   - 结论: 信息具有物理属性 (Information is Physical)。

3. 第 3 章终极结论:
   - 经典物理下的所有计算模型在复杂性意义上都是等价的 (强丘奇-图灵论题成立)。
   - 为第 4 章铺垫: 唯有利用量子力学特性的【量子计算机】，才能真正实现对经典图灵模型算力的物理级超越。

